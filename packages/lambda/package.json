{
    "name": "@cdk-constructs/lambda",
    "version": "0.1.0",
    "license": "Apache-2.0",
    "description": "Lambda function constructs with Node.js and Python support for CDK Constructs Library",
    "main": "dist/src/index.js",
    "types": "dist/src/index.d.ts",
    "keywords": [
        "aws-cdk",
        "lambda",
        "nodejs",
        "python",
        "serverless",
        "function"
    ],
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "rm -rf dist && tsc -p tsconfig.json",
        "build:noClean": "tsc -p tsconfig.json",
        "test": "jest",
        "clean": "rm -rf dist && tsc --build --clean",
        "format": "prettier --write ."
    },
    "peerDependencies": {
        "aws-cdk-lib": "^2.225.0",
        "constructs": "^10.0.0"
    },
    "devDependencies": {
        "@types/jest": "^29.5.14",
        "aws-cdk-lib": "2.225.0",
        "constructs": "10.4.2",
        "esbuild": "^0.21.0",
        "jest": "^29.7.0",
        "ts-jest": "^29.2.5",
        "typescript": "~5.4.3"
    },
    "engines": {
        "node": ">=24"
    }
}
