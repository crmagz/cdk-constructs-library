{
    "name": "@cdk-constructs/aurora",
    "version": "0.1.0",
    "license": "Apache-2.0",
    "description": "Aurora PostgreSQL and MySQL constructs for CDK Constructs Library",
    "main": "dist/src/index.js",
    "types": "dist/src/index.d.ts",
    "keywords": [
        "aws-cdk",
        "aurora",
        "postgresql",
        "mysql",
        "rds",
        "database"
    ],
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "rm -rf dist && tsc -p tsconfig.json",
        "build:noClean": "tsc -p tsconfig.json",
        "test": "jest",
        "synth": "npx cdk synth",
        "clean": "rm -rf dist && tsc --build --clean",
        "format": "prettier --write ."
    },
    "dependencies": {},
    "peerDependencies": {
        "aws-cdk-lib": "^2.225.0",
        "constructs": "^10.0.0"
    },
    "devDependencies": {
        "aws-cdk-lib": "2.225.0",
        "constructs": "10.4.2",
        "jest": "^30.2.0",
        "typescript": "~5.4.3"
    },
    "engines": {
        "node": ">=24"
    }
}
