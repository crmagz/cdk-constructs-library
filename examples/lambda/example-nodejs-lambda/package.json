{
    "name": "example-nodejs-lambda",
    "version": "1.0.0",
    "description": "Example Node.js Lambda for API Gateway integration",
    "main": "dist/index.js",
    "scripts": {
        "build": "rm -rf dist && tsc -p tsconfig.json",
        "watch": "tsc -w",
        "clean": "rm -rf dist && tsc --build --clean"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.145",
        "typescript": "~5.4.3"
    },
    "engines": {
        "node": ">=22"
    }
}
